# CutTheCord Environment Configuration
# Copy this file to docker/.env and update the values
# The setup.sh script does this automatically

# ── PostgreSQL ──
POSTGRES_USER=ctc
POSTGRES_PASSWORD=changeme_postgres_password
POSTGRES_DB=cutthecord

# ── Redis ──
REDIS_URL=redis://ctc-redis:6379

# ── JWT ──
# Generate with: openssl rand -base64 32
JWT_SECRET=changeme_jwt_secret_at_least_32_chars_long

# ── LiveKit (Voice/Video) ──
LIVEKIT_API_KEY=devkey
LIVEKIT_API_SECRET=changeme_livekit_secret_at_least_32_chars
# Use ws:// for LAN, wss:// if you have a TLS reverse proxy in front
LIVEKIT_URL=ws://YOUR_HOST_IP:7880

# ── Ports ──
WEB_PORT=3000
ADMIN_PORT=3001

# ── Host IP ──
# Your server's LAN IP address (used by LiveKit and client connections)
HOST_IP=YOUR_HOST_IP
